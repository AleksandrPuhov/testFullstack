import { type AccountWidgetItem } from "@/lib/utils/account";

export type AccountWidgetItemProps = AccountWidgetItem & {
  // type: "nal" | "bank" | "card" | "electron" | "acquiring";
  // id: number;
  // balance: number | null;
  // name: string;
  // isAvailable: boolean;
  // isAvailableGroupBalance: boolean;
  // hideInTotal: boolean;
};
